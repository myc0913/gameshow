---
description: A0 阶段 — 项目骨架与静态页面。见原文档§15对应小节。
---

# A0：项目骨架与静态页面

## 目标

先跑起来，不写复杂规则。

## 任务

- 初始化 Vite + React + TypeScript 项目。
- 创建 3 个 Tab：Play / How / Why。
- 创建基础 CSS 风格。
- 创建空的符文池、槽位、结果面板、动画区占位。

## 验收

- `npm run dev` 可启动。
- 默认进入 Play 页。
- 三个 Tab 可切换。
- 控制台无报错。
